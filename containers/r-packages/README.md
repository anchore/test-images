# `r-packages`
An R base image with specific (non-latest) versions of R packages installed for testing R package detection.

Packages:
- `commonmark` 1.8.1
- `gdata` 3.0.0
- `haven` 2.5.3
- `plotly` 4.10.0

Used in analyzer tests for R/CRAN package scanning.
